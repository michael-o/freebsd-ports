--- pyproject.toml.orig	2026-09-19 19:46:50 UTC
+++ pyproject.toml
@@ -1,5 +1,5 @@
 [build-system]
-requires = ["Cython>=3.2.9,<3.3", "setuptools"]
+requires = ["Cython>=3.2.9", "setuptools"]
 
 [tool.cibuildwheel]
 build-verbosity = 1
